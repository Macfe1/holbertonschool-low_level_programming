#include <stdio.h>
/**
*main - Principal Function
*
*Return: 0 if that is the anwser the succes
*/

int main(void)
{
int mero;
for (mero = 0; mero <= 9; mero++)
{
putchar('0' + mero);
}
putchar('\n');
return (0);
}
