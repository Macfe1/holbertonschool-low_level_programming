#include <stdio.h>
/**
*main - principal function
*
*Return: 0 if the 0 is the  answer then  success
*/
int main(void)
{
char alf = 'a';
char abeto = 'z';

while (alf <= abeto)
{
putchar(alf);
alf++;
}
putchar ('\n');
return (0);
}
